#include <iostream>
#include <bits/stdc++.h>
using namespace std;

#define l long
#define lu unsigned long
#define ll long long
#define ull unsigned long long
#define ld long double
#define ar array

int main() {
  int testcases;
  cin >> t;

  while (t--) {
    int diff;
    cin >> diff;

    // Pattern:
    // (X1) * (X2) * (X3) = (X4)
    int

  }


  return(0);
}
